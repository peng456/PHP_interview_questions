操作系统拥有的资源,其实就两样！！！
CPU、 存储（寄存器、内存、磁盘）。


进程的由来：
  刚开始计算机 就是用来计算数据的机器，功能只有 你输入数值，他计算，输出数值。很简单。可是后来要做的任务越来越多。也向着多任务系统发展。一台机器可以做很多工作
  比如图形画质、计算、网络通信等。
  为了任务直接不回想影响，所以发明进程这一概念，技术。每个进程负责一件事，在我的时间范围内，我的内存上，很自由，不会受到其他任务的干扰。类似操作系统给你提供了一个容器，你就在这个
  容器里面玩耍就OK了。
  
   内核态、用户态
   内核态：系统进程运行的时候，就叫内核态。
   用户态：用户级别进程运行的时候，就叫内核态。

   
   
  IO模型 是 对 内核态、用户态转化的 总结。
  
 
